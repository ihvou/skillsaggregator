node_modules
.next
.expo
dist
coverage
playwright-report
test-results
.validation
.collection
bin/yt-dlp
.env
.env.local
.env.*.local
supabase/.branches
supabase/.temp
*.tsbuildinfo
# Auto-generated by `npx expo install --fix` at the workspace root.
# The mobile app uses apps/mobile/tsconfig.json; this top-level file is harmless
# but recreates itself on every install, so we keep it untracked.
/tsconfig.json
